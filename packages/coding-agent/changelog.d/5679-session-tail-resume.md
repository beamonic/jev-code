### Fixed

- `gjc sdk session tail` now resumes without dropping observations: callers receive a signed continuation claim after replay/live follow, must provide the last processed transcript id on `--cursor` resumes, and transcript checkpoint failures fall back to the exchanged snapshot instead of returning partial success. Stopped-session tails apply the same boundary and fail with `retention_gap` when it is older than the bounded replay window.
- Live tail cursors advance after the delivered event position is finalized, so the next CLI invocation does not replay events already returned; frame collection freezes before the final watermark is minted, and a failed final mint fails closed instead of returning an unresumable success. Tail revision stamping retains the newest explicit revision across checkpoint races, and a bounded live wait returns all collected items with `terminal: false` rather than discarding them.
